server:
  port: 7000
eureka:
  instance:
    hostname: ec2-65-0-170-153.ap-south-1.compute.amazonaws.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka

